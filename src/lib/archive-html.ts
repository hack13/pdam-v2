import type { buildSyncManifest } from './sync-manifest';
import { renderMarkdown } from './markdown';

type ArchiveManifest = Awaited<ReturnType<typeof buildSyncManifest>>;

function escapeHtml(value: string) {
  return value.replace(/[&<>"']/g, (character) => ({ '&': '&amp;', '<': '&lt;', '>': '&gt;', '"': '&quot;', "'": '&#39;' })[character] ?? character);
}

function formatBytes(bytes: number) {
  if (bytes < 1024) return `${bytes} B`;
  if (bytes < 1024 * 1024) return `${(bytes / 1024).toFixed(1)} KB`;
  if (bytes < 1024 * 1024 * 1024) return `${(bytes / 1024 / 1024).toFixed(1)} MB`;
  return `${(bytes / 1024 / 1024 / 1024).toFixed(1)} GB`;
}

function filePath(asset: { slug: string }, version: { version: string }, file: { fileName: string; path?: string }) {
  if (file.path) return file.path;
  const safeName = file.fileName.replace(/[\\/]/g, '_').replace(/^\.+$/, '_');
  return `assets/${asset.slug}/${version.version}/${safeName}`;
}

export function buildArchiveHtml(manifest: ArchiveManifest) {
  const assetMarkup = manifest.assets.map((asset) => {
    const versions = asset.versions.map((version) => `
      <details class="version" open>
        <summary><b>${escapeHtml(version.version)}</b><span>${version.files.length} file${version.files.length === 1 ? '' : 's'}${version.publishedAt ? ` · ${escapeHtml(new Date(version.publishedAt).toLocaleDateString())}` : ''}</span></summary>
        ${version.releaseNotes ? `<p class="notes">${escapeHtml(version.releaseNotes)}</p>` : ''}
        <ul>${version.files.map((file) => `<li><a href="${escapeHtml(filePath(asset, version, file))}">${escapeHtml(file.fileName)}</a><small>${escapeHtml(file.mimeType)} · ${formatBytes(file.byteSize)}</small></li>`).join('')}</ul>
      </details>`).join('');
    const thumb = asset.thumbnailPath ? `<img class="thumb" src="${escapeHtml(asset.thumbnailPath)}" alt="">` : '<div class="thumb missing" aria-hidden="true">no thumb</div>';
    const descriptionHtml = asset.descriptionText ? renderMarkdown(asset.descriptionText) : '';
    const license = asset.licenseKey ? `<div class="license"><span>license key</span><code>${escapeHtml(asset.licenseKey)}</code></div>` : '';
    return `<article class="asset" data-search="${escapeHtml(`${asset.title} ${asset.descriptionText ?? ''} ${(asset.tags ?? []).join(' ')}`.toLowerCase())}">${thumb}<div class="asset-body"><div class="asset-title"><h2>${escapeHtml(asset.title)}</h2><span>${asset.versions.length} version${asset.versions.length === 1 ? '' : 's'}</span></div>${descriptionHtml ? `<div class="description markdown">${descriptionHtml}</div>` : ''}${asset.tags?.length ? `<p class="tags">${asset.tags.map((tag) => `#${escapeHtml(tag)}`).join(' ')}</p>` : ''}${license}<div class="versions">${versions || '<p class="muted">No versions in this backup.</p>'}</div></div></article>`;
  }).join('');

  const generated = new Date(manifest.generatedAt).toLocaleString();
  return `<!doctype html>
<html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>my TailCache archive</title>
<style>
*{box-sizing:border-box}html{background:#d9d4c6}body{margin:0;overflow-x:hidden;color:#342f2b;background:#e9e4d8;font:14px/1.5 system-ui,-apple-system,BlinkMacSystemFont,"Segoe UI",Verdana,Arial,sans-serif}a{color:#245c78}a:hover{color:#a34036}.site{width:min(calc(100% - 32px),1120px);margin:18px auto 40px;border:1px solid #9b9486;background:#f4f0e5;box-shadow:5px 5px 0 #a39b8e}.top{padding:25px 28px 18px;background:#35677b;color:#fff;border-bottom:5px solid #d7a93d}.top h1{margin:0;color:#fff;font:bold 34px Georgia,serif;letter-spacing:-1px;overflow-wrap:anywhere}.top p{margin:5px 0 0;color:#e9f0ed}.top .meta{float:right;margin-top:-35px;text-align:right;color:#d9e7e4;font:11px 'Courier New',monospace}.layout{display:grid;grid-template-columns:minmax(150px,165px) minmax(0,1fr);min-height:560px}.side{padding:20px 15px;background:#d4dfd5;border-right:1px dashed #8e9d91}.side h3{margin:0 0 12px;color:#6b332d;font:bold 16px Georgia,serif}.side p{font-size:12px;overflow-wrap:anywhere}.side .badge{display:block;margin-top:24px;padding:7px;border:1px solid #849889;background:#e7efe2;color:#506655;font:11px 'Courier New',monospace;overflow-wrap:anywhere}.main{min-width:0;padding:22px 25px}.main h3{margin:0 0 14px;padding-bottom:7px;border-bottom:2px dotted #b2aa9c;color:#6b332d;font:bold 19px Georgia,serif}.search{display:block;width:100%;padding:9px 10px;margin-bottom:16px;border:1px solid #a9a092;background:#fffdf7;color:#342f2b;font:13px system-ui,-apple-system,BlinkMacSystemFont,"Segoe UI",Verdana,Arial,sans-serif}.asset{display:grid;grid-template-columns:112px minmax(0,1fr);gap:17px;min-width:0;margin:0 0 17px;padding:13px;background:#fffdf7;border:1px solid #b9b0a1;box-shadow:2px 2px 0 #d0c8bb}.asset-body{min-width:0}.thumb{width:112px;height:112px;object-fit:cover;border:1px solid #817b70;background:#c8d3cc;image-rendering:auto}.thumb.missing{display:flex;align-items:center;justify-content:center;color:#68756b;font:11px 'Courier New',monospace}.asset-title{display:flex;align-items:flex-start;justify-content:space-between;gap:12px;min-width:0;border-bottom:1px solid #d5cec1}.asset-title h2{min-width:0;margin:0 0 5px;color:#2d5361;font:bold 21px Georgia,serif;overflow-wrap:anywhere}.asset-title span,.asset small,.asset-title~span{flex:0 0 auto;color:#776f67;font:11px 'Courier New',monospace}.description{margin:7px 0;color:#4d4842;overflow-wrap:anywhere}.tags{margin:8px 0;color:#a34036;font:12px 'Courier New',monospace;overflow-wrap:anywhere}.version{min-width:0;margin-top:11px;border:1px solid #d5cec1;background:#f7f3e9}.version summary{display:flex;align-items:baseline;gap:10px;cursor:pointer;padding:7px 8px;list-style:'+ ';color:#6b332d}.version[open] summary{list-style:'- '}.version summary b{min-width:0;overflow-wrap:anywhere}.version summary span{margin-left:auto;flex:0 0 auto;color:#776f67;font:11px 'Courier New',monospace;text-align:right}.notes{margin:8px;color:#6b6259;font-style:italic;overflow-wrap:anywhere}.version ul{min-width:0;margin:0;padding:0 8px 8px;list-style:none}.version li{display:flex;align-items:baseline;justify-content:space-between;min-width:0;gap:15px;padding:6px 2px;border-top:1px dotted #d5cec1}.version li a{min-width:0;overflow-wrap:anywhere;word-break:break-word;white-space:normal}.version li small{flex:0 1 38%;max-width:38%;color:#776f67;font:11px 'Courier New',monospace;text-align:right;overflow-wrap:anywhere}.muted,.none{color:#776f67}.footer{padding:13px 25px;border-top:1px dashed #b2aa9c;color:#776f67;font:11px 'Courier New',monospace;overflow-wrap:anywhere}@media(max-width:700px){.site{width:100%;margin:0;border-width:0;box-shadow:none}.layout{display:block}.side{border-right:0;border-bottom:1px dashed #8e9d91}.top .meta{float:none;margin:12px 0 0;text-align:left}.asset{grid-template-columns:76px minmax(0,1fr);gap:11px}.thumb{width:76px;height:76px}.version summary{display:block}.version summary span{display:block;margin:3px 0 0;text-align:left}.version li{display:block}.version li a{display:block}.version li small{display:block;max-width:none;margin-top:3px;text-align:left}}
</style><style>.markdown p{margin:7px 0}.markdown h1,.markdown h2,.markdown h3,.markdown h4,.markdown h5,.markdown h6{margin:10px 0 5px;color:#6b332d;font-family:Georgia,serif}.markdown ul,.markdown ol{margin:7px 0;padding-left:20px}.markdown blockquote{margin:8px 0;padding-left:10px;border-left:3px solid #d7a93d;color:#6b6259}.markdown code{padding:1px 4px;background:#e9e4d8;font:12px 'Courier New',monospace}.markdown pre{overflow:auto;padding:9px;background:#e9e4d8;border:1px solid #d5cec1}.markdown a{overflow-wrap:anywhere}.license{display:grid;grid-template-columns:auto minmax(0,1fr);gap:8px;align-items:start;margin:10px 0;padding:8px;background:#eef1e8;border:1px dashed #849889}.license span{color:#506655;font:11px 'Courier New',monospace;text-transform:uppercase}.license code{min-width:0;color:#342f2b;font:12px 'Courier New',monospace;overflow-wrap:anywhere;user-select:all}.asset-body,.asset-title,.version,.version ul{min-width:0}.asset-title h2,.version summary b{overflow-wrap:anywhere}.version summary{display:flex;align-items:baseline;gap:10px}.version summary span{margin-left:auto;flex:0 0 auto;text-align:right}.version li a{min-width:0;overflow-wrap:anywhere;word-break:break-word;white-space:normal}.version li small{flex:0 1 38%;max-width:38%;overflow-wrap:anywhere;text-align:right}@media(max-width:700px){.license{grid-template-columns:1fr;gap:4px}.version summary{display:block}.version summary span{display:block;margin:3px 0 0;text-align:left}.version li{display:block}.version li a{display:block}.version li small{display:block;max-width:none;margin-top:3px;text-align:left}}</style></head><body><div class="site"><header class="top"><div class="meta">exported<br>${escapeHtml(generated)}</div><h1>my TailCache archive</h1><p>stuff i have collected · versions · files</p></header><div class="layout"><aside class="side"><h3>hello!</h3><p>This is a local copy of my archive. Everything here is meant to work from this folder, even when the server is offline.</p><p>Click a version to see its files. Click a file name to open it.</p><span class="badge">made with TailCache<br>saved locally ♡</span></aside><main class="main"><h3>the collection</h3><input class="search" id="search" type="search" placeholder="filter the collection..." aria-label="Filter the collection"><div id="items">${assetMarkup || '<p class="none">nothing here yet!</p>'}</div><p class="none" id="empty" hidden>nothing matched that filter.</p></main></div><footer class="footer">Archive.html · ${manifest.assets.length} item${manifest.assets.length === 1 ? '' : 's'} · no internet connection required</footer></div><script>const input=document.getElementById('search'),items=[...document.querySelectorAll('.asset')],empty=document.getElementById('empty');input.addEventListener('input',()=>{const q=input.value.trim().toLowerCase();let shown=0;items.forEach(item=>{const match=!q||item.dataset.search.includes(q);item.hidden=!match;if(match)shown++});empty.hidden=shown!==0})</script></body></html>`;
}
