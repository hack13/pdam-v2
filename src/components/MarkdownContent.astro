---
import { renderMarkdown } from '../lib/markdown';

interface Props {
  content: string;
  class?: string;
}

const { content, class: className = '' } = Astro.props;
const html = renderMarkdown(content);
---

{html && <div class:list={['markdown-content', className]} set:html={html} />}
